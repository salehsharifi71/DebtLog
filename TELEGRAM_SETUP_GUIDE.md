# 📱 راهنمای تنظیم اعلان‌های تلگرام

## مرحله 1️⃣: ساخت ربات تلگرام

1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام دهید
2. دستور `/newbot` را ارسال کنید
3. یک نام برای ربات خود انتخاب کنید (مثال: **سیستم مدیریت اقساط من**)
4. یک username منحصر به فرد برای ربات انتخاب کنید (باید به `bot` ختم شود، مثال: `my_expense_manager_bot`)
5. BotFather یک **توکن** به شما می‌دهد که شبیه این است:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
6. این توکن را کپی کنید

## مرحله 2️⃣: دریافت Chat ID

### روش اول (استفاده از @userinfobot):
1. در تلگرام به [@userinfobot](https://t.me/userinfobot) پیام دهید
2. ربات Chat ID شما را نمایش می‌دهد (مثال: `123456789`)

### روش دوم (دستی):
1. ابتدا به ربات خود پیام `/start` ارسال کنید
2. سپس این URL را در مرورگر باز کنید:
   ```
   https://api.telegram.org/bot{YOUR_BOT_TOKEN}/getUpdates
   ```
3. در نتیجه JSON، مقدار `"chat":{"id":123456789}` را پیدا کنید

## مرحله 3️⃣: تنظیم فایل .env

فایل `.env` پروژه لاراول خود را باز کنید و این دو خط را اضافه کنید:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

⚠️ **نکته**: مقادیر بالا را با توکن و Chat ID واقعی خود جایگزین کنید.

## مرحله 4️⃣: تست ارسال پیام

### تست دستی دستور:
```bash
php artisan app:send-due-notifications
```

### تست با ایجاد قسط آزمایشی:
1. یک قسط جدید با تاریخ سررسید فردا ایجاد کنید
2. دستور بالا را اجرا کنید
3. باید پیام در تلگرام دریافت شود

## مرحله 5️⃣: تنظیم Scheduler برای اجرای خودکار

فایل `routes/console.php` را باز کرده و این کد را اضافه کنید:

```php
use Illuminate\Support\Facades\Schedule;

Schedule::command('app:send-due-notifications')
    ->dailyAt('09:00') // هر روز ساعت 9 صبح
    ->timezone('Asia/Tehran');
```

یا می‌توانید در `app/Console/Kernel.php` (اگر لاراول 10 به پایین دارید):

```php
protected function schedule(Schedule $schedule)
{
    $schedule->command('app:send-due-notifications')
             ->dailyAt('09:00')
             ->timezone('Asia/Tehran');
}
```

## مرحله 6️⃣: فعال‌سازی Task Scheduler

### در لینوکس (با Cron):
فایل crontab را با دستور `crontab -e` باز کرده و این خط را اضافه کنید:

```bash
* * * * * cd /path/to/your/project && php artisan schedule:run >> /dev/null 2>&1
```

### در ویندوز (با Task Scheduler):
1. Task Scheduler را باز کنید
2. یک Task جدید ایجاد کنید که این دستور را هر دقیقه اجرا کند:
   ```
   php d:\project\personal-accounting\artisan schedule:run
   ```

### در محیط development:
برای تست، می‌توانید این دستور را اجرا کنید:
```bash
php artisan schedule:work
```

## 📋 دستورات مفید

```bash
# اجرای دستی دستور
php artisan app:send-due-notifications

# مشاهده لیست تمام دستورات
php artisan list

# مشاهده لیست scheduledهای فعال
php artisan schedule:list

# اجرای scheduler به صورت مداوم (برای توسعه)
php artisan schedule:work
```

## 🎯 نمونه پیام دریافتی در تلگرام

```
⏰ یادآوری سررسید قسط

📅 عنوان: وام مسکن - قسط 5
💰 مبلغ: 10,000,000 ریال
📆 تاریخ سررسید: 1403/09/23
🔢 شماره قسط: 5/12

⚠️ این قسط فردا سررسید دارد!
```

## 🔧 عیب‌یابی

### مشکل: پیام ارسال نمی‌شود
- بررسی کنید که توکن و Chat ID صحیح باشد
- مطمئن شوید که به ربات پیام `/start` داده‌اید
- لاگ‌های `storage/logs/laravel.log` را بررسی کنید

### مشکل: Chat ID اشتباه است
- حتماً باید ابتدا به ربات پیام `/start` بدهید
- از @userinfobot استفاده کنید تا Chat ID خود را دریافت کنید

### مشکل: دستور schedule اجرا نمی‌شود
- مطمئن شوید که cron job یا Task Scheduler به درستی تنظیم شده است
- با دستور `php artisan schedule:work` تست کنید

## 📚 منابع بیشتر

- [مستندات رسمی Telegram Bot API](https://core.telegram.org/bots/api)
- [مستندات Laravel Task Scheduling](https://laravel.com/docs/scheduling)
- [پکیج telegram-bot-sdk](https://github.com/irazasyed/telegram-bot-sdk) (پیشرفته‌تر)

---

✅ **تبریک! سیستم اعلان تلگرام شما آماده است.**
