<div dir="rtl">

# 🔐 سیستم احراز هویت (Authentication)

## مقدمه

سیستم DebtLog دارای سیستم ورود ساده و امن است که برای کاربران احراز هویت را فراهم می‌کند. **ثبت نام خودکار پشتیبانی نمی‌شود** و باید توسط مدیر سیستم انجام شود.

## نحوه ورود

### 1️⃣ دسترسی به صفحه ورود

- آدرس صفحه ورود: `http://localhost:8000/login`
- هر کاربری که سعی کند بدون ورود وارد سیستم شود، به صفحه ورود ریدایرکت خواهد شد

### 2️⃣ اطلاعات ورود کاربر تست

برای تست کردن سیستم از اطلاعات زیر استفاده کنید:

```
📧 ایمیل: test@debtlog.local
🔑 رمز عبور: password123
```

### 3️⃣ مراحل ورود

1. ایمیل خود را در فیلد **ایمیل** وارد کنید
2. رمز عبور را در فیلد **رمز عبور** وارد کنید
3. اگر می‌خواهید دستگاه را یادآوری کنند، گزینه **یادآوری من بمان** را فعال کنید
4. روی دکمه **ورود به سیستم** کلیک کنید

## مدیریت کاربران

### اضافه کردن کاربر جدید

برای اضافه کردن کاربر جدید، باید مستقیماً در دیتابیس ثبت شود. استفاده از دستور Artisan:

```bash
php artisan tinker
```

سپس:

```php
use App\Models\User;
use Illuminate\Support\Facades\Hash;

User::create([
    'name' => 'نام کاربر',
    'email' => 'user@example.com',
    'password' => Hash::make('password123'),
]);
```

### تغییر رمز عبور

```php
use App\Models\User;

$user = User::where('email', 'user@example.com')->first();
$user->update(['password' => Hash::make('new_password')]);
```

## امنیت

### ویژگی‌های امنیتی

- ✅ **رمزگذاری رمز عبور**: تمام رمزها با Hash کردن ذخیره می‌شوند
- ✅ **جلسات امن**: از Session‌های رمزگذاری شده استفاده می‌شود
- ✅ **CSRF Protection**: تمام فرم‌ها توسط CSRF Token حفاظت می‌شوند
- ✅ **Rate Limiting**: (در آینده) محدود کردن تعداد تلاش‌های ورود

### بهترین روش‌ها

1. **رمز قوی انتخاب کنید**
   - حداقل 12 کاراکتر
   - ترکیب حروف بزرگ و کوچک
   - شامل اعداد و نویسه‌های خاص

2. **رمز خود را مخفی نگاه دارید**
   - هرگز رمز را با دیگری به‌اشتراک نگذارید
   - رمز را در جای امن ذخیره کنید

3. **منقضی کردن رمز**
   - رمز خود را به طور منظم تغییر دهید

## خروج از سیستم

برای خروج از سیستم:

1. روی نام کاربر در نوار بالای صفحه کلیک کنید
2. دکمه **خروج** را کلیک کنید

## حل مشکلات

### مشکل: "اطلاعات ورود نامعتبر است"

**دلایل احتمالی:**
- ایمیل یا رمز عبور نادرست است
- کاربر هنوز ایجاد نشده است

**راه‌حل:**
- ایمیل و رمز عبور را دوباره بررسی کنید
- از مدیر سیستم درخواست کنید کاربر را اضافه کند

### مشکل: صفحه ورود بارها بازگردانده می‌شود

**دلایل احتمالی:**
- Session منقضی شده است
- کوکی‌ها بسته شده‌اند

**راه‌حل:**
- کوکی‌های مرورگر را پاک کنید
- مرورگر را بازشروع کنید

### مشکل: نمی‌توانم رمز خود را بازیابی کنم

**راه‌حل:**
- با مدیر سیستم تماس بگیرید
- رمز جدید را از طریق Tinker تعیین کنید

## مراجع

- [مستندات Laravel Authentication](https://laravel.com/docs/authentication)
- [نمایش کاربر فعلی](https://laravel.com/docs/authentication#retrieving-the-authenticated-user)

</div>
